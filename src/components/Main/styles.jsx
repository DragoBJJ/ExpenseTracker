import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => ({
  cartContent: {
    paddingTop: "10px"
  },
  divider: {
    margin: "20px 0"
  },
  subtitle: {
    lineHeight: "1.5em",
    marginTop: "20px",
    textAlign: "center"
  }
}));
